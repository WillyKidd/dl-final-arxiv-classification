# dl-final-arxiv-classification

Get started:

```shell
python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt

python preprocess.py

python train.py
```

Formatting

```shell
pre-commit run --all-files
```
